import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'stretch',
    flexDirection: 'row',
    height: 50,
    marginBottom: 1,
  },
  input: {
    flex: 1,
  },
  button: {
    justifyContent: 'center',
  },
});
