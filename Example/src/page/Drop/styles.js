import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  listItem: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    marginTop: 40,
  },
});

export default styles;
